export default `
<main class="chat">
    <div class="left-panel">
        {{{ link }}}

        {{{ search }}}

        {{{ contactContainer }}}
    </div>

    <div class="all-message">
        <div class="container">
            {{{ messageBubbleOne }}}
            {{{ messageBubbleTwo }}}
            {{{ messageBubbleThree }}}
        </div>

        {{{ messageInput }}}
    </div>
</main>
`
