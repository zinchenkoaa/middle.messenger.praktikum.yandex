export default `
<div class="registration">
    <div class="registration-inputs">
        {{{ mailInput }}}
        {{{ loginInput }}}
        {{{ firstNameInput }}}
        {{{ secondNameInput }}}
        {{{ phoneInput }}}
        {{{ passwordInput }}}
    </div>
    <div class="registration-footer">
        {{{ button }}}

        {{{ link }}}
    </div>
</div>
`
